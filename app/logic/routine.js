const dbContext = require('./dbContext');
const teachingCache = require('./teaching-cache');
const templateCache = require('./template-cache');
const userMng = require('./user');

let routineMng = {
  startRoutine: function(userID, tchRoutineID) {
            
  },
  
  getNavigationContext: function(tchRoutineID, tchNodeID) {

  }
}